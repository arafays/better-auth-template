// you might as why I am using .ts file instead of d.ts becuase I want to use enums as values because that is what I like

export enum UserRole {
  ADMIN = "admin",
  USER = "user",
}

// export type SiteMetadata = {}
